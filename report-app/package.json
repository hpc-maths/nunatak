{
  "name": "nunatak-report-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
