# Deployment

For whoever administers the machine rather than profiles on it: the
kernel settings that decide whether measurement is possible at all, the
site's own tools, and the files that set defaults for every user.

A reader who cannot change the machine lands here from a degradation's
entry in the reference, which names what was lost and points at the page
that fixes it.

```{toctree}
:maxdepth: 1

kernel-permissions
powermetrics
```
