# Source

A Run carries the code its Hotspots point at, so that a report can be
read on a machine where the repository does not exist. The recipe is
about making the files findable and current; the page after it says what
is kept, what is refused, and why a wrong extract is worse than none.

```{toctree}
:maxdepth: 1

get-the-source-into-your-run
what-the-run-keeps-of-your-code
```
