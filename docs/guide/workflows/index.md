# Workflows

Three situations no single subject covers, and the mistakes that survive
nunatak's own checks.

Each recipe below composes what the subjects explain one at a time. Where
a step rests on a mechanism rather than on a flag, the subject that owns
that mechanism is linked from the step.

```{toctree}
:maxdepth: 1

profile-a-job-on-a-scheduler
gate-performance-in-ci
profile-where-source-cannot-leave
common-pitfalls
```
