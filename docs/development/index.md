# Development

For whoever changes nunatak rather than uses it: how the pieces are cut,
what each one may know about the others, and what the test suite can and
cannot promise.

The specification these pages inherit is being retired chapter by
chapter, in the pull request that writes the page replacing it.

```{toctree}
:maxdepth: 1

architecture
code-map
testing
decisions/index
```
